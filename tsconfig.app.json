{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [] // Isso pode ser mantido aqui ou em tsconfig.json, ambos funcionam com extends
  },
  "include": [ // <--- Use 'include' para todos os arquivos .ts
    "src/**/*.ts"
  ],
  "exclude": [ // <--- É bom excluir arquivos de teste explicitamente
    "src/**/*.spec.ts"
  ]
}
